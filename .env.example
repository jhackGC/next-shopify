# Environment variables for Shopify integration
# Copy this file to .env.local or env.prod and fill in your actual values

ENV=dev/prod

# Shopify Store Configuration (Server-side only - not exposed to browser)
# Domain should be without https:// prefix (it will be added automatically)
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# Shopify Storefront API Access Tokens
# Both tokens should have the same permissions
SHOPIFY_STOREFRONT_PRIVATE_ACCESS_TOKEN=your_private_storefront_access_token
SHOPIFY_STOREFRONT_PUBLIC_ACCESS_TOKEN=your_public_storefront_access_token

# Shopify Customer Account API
SHOPIFY_CUSTOMER_ACCOUNT_API_CLIENT_ID=your_client_id_from_partner_dashboard
SHOPIFY_CUSTOMER_ACCOUNT_API_CLIENT_SECRET=your_client_secret_from_partner_dashboard

# Get this URL from Partner Dashboard → Your App → Customer Account API settings
# Format: https://shopify.com/{shop-id}/account/customer/api/2025-07/graphql
SHOPIFY_CUSTOMER_ACCOUNT_API_URL=https://shopify.com/{your-shop-id}/account/customer/api/2025-07/graphql


##### Application endpoints #####
## For the tokens to work, they have to be same domain
## The endpoint you are authorising the user, and the token retrieval endpoint and the logout, 
## they all have to be the same domain as SHOPIFY_CUSTOMER_ACCOUNT_API_URL, e.g. https://shopify.com...


# Authorization endpoint
SHOPIFY_CUSTOMER_ACCOUNT_API_AUTHORIZATION_ENDPOINT=https://shopify.com/authentication/{your-shop-id}/oauth/authorize
# Tokens endpoint
SHOPIFY_CUSTOMER_ACCOUNT_API_TOKEN_ENDPOINT=https://shopify.com/authentication/{your-shop-id}/oauth/token
# Logout endpoint
SHOPIFY_CUSTOMER_ACCOUNT_API_LOGOUT_ENDPOINT=https://shopify.com/authentication/{your-shop-id}/logout

# Application domain (Private - needed for server-side)
APP_URL=http://localhost:3000 or your prod app domain
PROXIED_LOCALHOST_APP_URL=https://your-proxied-localhost-url for auth dev test with ngrok

# Application domain (Public - needed for client-side)
NEXT_PUBLIC_APP_URL=http://localhost:3000 ## @TODO double check if needed

# SHOPIFY_CUSTOMER_ACCOUNT_API_AUTHORIZATION_ENDPOINT=https://account.{your-shop-domain}/authentication/oauth/authorize
# Tokens endpoint
# SHOPIFY_CUSTOMER_ACCOUNT_API_TOKEN_ENDPOINT=https://account.{your-shop-domain}/authentication/oauth/token
# Logout endpoint
# SHOPIFY_CUSTOMER_ACCOUNT_API_LOGOUT_ENDPOINT=https://account.{your-shop-domain}/authentication/logout
